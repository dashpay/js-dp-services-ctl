const startDashCoreInstance = require('./startDashCoreInstance');
const startDashDriveInstance = require('./startDashDriveInstance');
const startIPFSInstance = require('./startIPFSInstance');
const startMongoDbInstance = require('./startMongoDbInstance');

module.exports = {
  startDashCoreInstance,
  startDashDriveInstance,
  startIPFSInstance,
  startMongoDbInstance,
};
