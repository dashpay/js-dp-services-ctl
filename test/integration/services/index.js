/* eslint-disable global-require */
describe('Services', () => {
  require('./dashCore');
  require('./driveApi');
  require('./driveSync');
  require('./IPFS');
  require('./mongoDb');
  require('./startDashDrive');
});
