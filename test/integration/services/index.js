/* eslint-disable global-require */
describe('Services', () => {
  require('./dashCore');
  require('./driveApi');
  require('./mongoDb');
  require('./startDrive');
  require('./startDapi');
  require('./node');
  require('./tendermintCore');
});
